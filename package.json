{
  "name": "t4al",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta 'npm:dev --workspace=@t4al/server' 'npm:dev --workspace=@t4al/client'",
    "puppet": "tsx tools/puppet.ts",
    "demo:admin": "node tools/demo-admin.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "concurrently": "^9.0.0",
    "typescript": "^5.7.3"
  }
}
